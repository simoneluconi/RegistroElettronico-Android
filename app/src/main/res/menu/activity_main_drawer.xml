<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/medie"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/medie" />

        <item
            android:id="@+id/communications"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/communications" />
        <item
            android:id="@+id/notes"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/note" />
        <item
            android:id="@+id/absences"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/absences" />
    </group>

    <item android:title="@string/communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="@string/share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/send" />
        </menu>
    </item>

</menu>
